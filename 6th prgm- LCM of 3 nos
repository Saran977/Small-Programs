def gcd(a,b):
    while(b!=0):
        remainder=a%b
        temp=b
        a=b 
        b=remainder
    return a

def lcm(a,b):
    return (a*b)/gcd(a,b)

a=int(input("Enter no 1"))
b=int(input("Enter no 2"))
c=int(input("Enter no 3"))

d1=lcm(a,b)
d2=lcm(d1,c)
print("LCM= ",d2)
